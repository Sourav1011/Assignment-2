<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>To-Do List</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <h1>To-Do List</h1>

    <a href="/add-task">➕   Add a New Task</a>

    
    <ul>
        <% tasks.forEach(task => { %>
            <li>
                 <a href="/task?id=<%= task.id %>"><%= task.description %></a>
               
                <a href="/delete-task?id=<%= task.id %>" onclick="return confirm('Are you sure you want to delete this task?');">
                    ❌ Delete
                </a>
            </li>
        <% }); %>
    </ul>

</body>
</html>

